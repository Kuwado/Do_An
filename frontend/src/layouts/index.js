import UserDefault from './user/Default/UserDefault';

const layouts = {
    admin: {
        // default: AdminLayout,
    },

    user: {
        default: UserDefault,
    },

    other: {
        // noHeader: NoHeaderLayout,
    },
};

export default layouts;
